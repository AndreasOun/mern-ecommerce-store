{
  "name": "ecommerce",
  "version": "1.0.0",
  "description": "Live preview is available on Heroku",
  "main": "index.js",
  "scripts": {
    "build": "cd ecommerce-backend && npm install && cd ../ecommerce-frontend && npm install && npm run build --force",
    "start": "node backend/server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC"
}
